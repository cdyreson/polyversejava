
import java.io.IOException;

/**
 *
 * @author Dyreson
 */
public class test1 {
    
    public static void main( String[] args) throws IOException
    {
        int x = 0;
        
        
        test(allTime,x);
        
    }
    
    
  public static void test(TimeMap timeMap,int x) {
      
      sequenced.Integer y = new sequenced.Integer(timeMap,0);
      
      sequenced.Integer z = new sequenced.Integer(timeMap,0);
      
      sequenced.Integer a = new sequenced.Integer(timeMap,0);
      
      
      a.assign(allTime,z.add(allTime,y));
      
  }
}
